import "../styles/main.scss";

$(document).ready(function () {
  // Весь ваш код jQuery должен быть внутри этой функции

  // 1. Выбираем элемент с ID 'greeting' и меняем его текст.
  $("#greeting").text("Привет от jQuery!");
});
